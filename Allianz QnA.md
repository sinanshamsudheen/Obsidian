how do we handle 100M users?
are we gonna show real time heartbeat by fetching from timescaledb in realtime? wont that cost us alot of API calls?

when do we use the agents? when an alert is generated? are we gonna use the agents as a microservice so that they only run while being used?
is there any cold start areas in the system?
where alll shall we implement redis cache?
how can we reduce latency even more?
can u list 10 example categorization for cohort models?
how effective is river in this system?
wt all can our chatbot do?
